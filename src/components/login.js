import React from "react";

const Login = () => {
  return <>
    <h1>Login Page</h1>
    <p>
      Customers information will be stored here.
      <br/>
      Need to create a db to store users and so information can be stored and pulled from
    </p>
  </>;
};

export default Login;
